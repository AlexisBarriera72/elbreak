# Lo que genera el build. Los hosts lo reconstruyen en cada despliegue.
dist/

# Variables locales. Nunca se suben.
.env
.env.local
.dev.vars

# Cachés de los hosts
.vercel/
.netlify/
.wrangler/

# Node (este proyecto no tiene dependencias, pero por si acaso)
node_modules/
npm-debug.log*

# Sistema
.DS_Store
Thumbs.db
